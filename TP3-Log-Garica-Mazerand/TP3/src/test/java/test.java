import org.junit.Test;

import static com.mongodb.internal.connection.tlschannel.util.Util.assertTrue;
import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;
import org.bson.Document;
import java.time.LocalDate;
import org.bson.types.ObjectId;
import java.time.LocalDate;
import java.util.Scanner;
import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;
import org.bson.Document;
import java.time.LocalDate;
import org.junit.Test;
import static com.mongodb.internal.connection.tlschannel.util.Util.assertTrue;
//public class test {
//    @Test
//    public void testLoggerInjection() {
//        // Charge une classe modifiée
//        CtClass<?> ctClass = launcher.getFactory().Class().get("com.example.UserService");
//        // Vérifie qu'un champ logger est présent
//        assertTrue(ctClass.getFields().stream()
//                .anyMatch(field -> field.getSimpleName().equals("logger")));
//    }
//}
